
/**
 * knex migrations and seed script
 * CORS policy
 * DB singleton connection
 * Move to TS create interface and enums export it.
 * move the middleware out
 * Use classes and static functions if required
 */


 move DB logic to models call the class methods use proper interface and return types

 jest https://www.npmjs.com/package/jest


code coverage